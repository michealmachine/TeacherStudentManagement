<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhangziqi.dao.StudentCourseMapper">
  <resultMap id="BaseResultMap" type="com.zhangziqi.entity.StudentCourseKey">
    <id column="sid" jdbcType="INTEGER" property="sid" />
    <id column="cid" jdbcType="INTEGER" property="cid" />
    <id column="tid" jdbcType="INTEGER" property="tid" />
  </resultMap>
  <update id="updateByKey">
      update studentcourse
      <set>
          <if test="sid != null">
              sid = #{sid},
          </if>
          <if test="tid != null">
                tid=#{tid},
          </if>
          <if test=" cid != null">
                cid=#{cid},
          </if>
      </set>
  </update>
  <delete id="deleteByPrimaryKey" parameterType="com.zhangziqi.entity.StudentCourseKey">
    delete from studentcourse
    where sid = #{sid,jdbcType=INTEGER}
      and cid = #{cid,jdbcType=INTEGER}
      and tid = #{tid,jdbcType=INTEGER}
  </delete>
  <select id="selectByStudentCourseKey" resultType="com.zhangziqi.entity.StudentCourseKey">
    select * from studentcourse where sid = #{sid} and cid = #{cid} and tid = #{tid};
  </select>
    <select id="selectStudentAndTeacher" resultType="com.zhangziqi.entity.StudentCourse">
        select sid,studentcourse.cid,studentcourse.tid,student.name as studentName,course.name as courseName from studentcourse,student,course where studentcourse.sid=student.id and course.tid=studentcourse.tid and course.cid=studentcourse.cid
    </select>
    <insert id="insert" parameterType="com.zhangziqi.entity.StudentCourseKey">
    insert into studentcourse (sid, cid, tid
      )
    values (#{sid,jdbcType=INTEGER}, #{cid,jdbcType=INTEGER}, #{tid,jdbcType=INTEGER}
      )
  </insert>
  <insert id="insertSelective" parameterType="com.zhangziqi.entity.StudentCourseKey">
    insert into studentcourse
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="sid != null">
        sid,
      </if>
      <if test="cid != null">
        cid,
      </if>
      <if test="tid != null">
        tid,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="sid != null">
        #{sid,jdbcType=INTEGER},
      </if>
      <if test="cid != null">
        #{cid,jdbcType=INTEGER},
      </if>
      <if test="tid != null">
        #{tid,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
</mapper>